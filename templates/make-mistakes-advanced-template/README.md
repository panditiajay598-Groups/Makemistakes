# MakeMistakes Advanced Project

Isolated BuildOS workspace for Advanced problem statements.

## Pre-installed (automatic)

Everything from Intermediate, plus:

- `axios` — HTTP client
- `react-hook-form` + `@hookform/resolvers` — forms
- `papaparse` — CSV
- `xlsx` — spreadsheets
- `immer` — immutable updates
- `nanoid` — ids

Still no local install on your laptop — packages live inside your BuildOS workspace only.
